events {}
http {
    include /etc/nginx/mime.types;
    server {
        listen ${PORT_CLIENT_IN};
        server_name shopping_pathfinding;
        root /usr/share/nginx/html;
        index index.html;
    }
}